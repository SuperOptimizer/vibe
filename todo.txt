VIBE TODO LIST
==============

HIGH PRIORITY
-------------
[X] Implement multi-entry TLB (16-64 entries) - COMPLETED
    - Implemented 32-entry fully-associative TLB
    - Round-robin replacement policy for simplicity and predictability
    - ASID support for better context switching
    - Proper invalidation on SATP writes and sfence.vma
    - Should provide 5-10x performance improvement for fuzzing workloads

[ ] Add RTC device for timekeeping
    - Required for kernel coverage collection timestamps
    - Can be simple memory-mapped registers

[ ] Implement RNG/virtio-rng for kernel entropy
    - Kernel needs entropy for ASLR and security features
    - virtio-rng is standard approach

MEDIUM PRIORITY
---------------
[ ] Increase RAM from 128MB to 256MB+
    - Current 128MB might be tight for kernel + syzkaller overhead
    - Simple change in MACH_RAM_SIZE

[ ] Add instruction cache
    - Cache decoded instructions to avoid re-decoding
    - Significant performance improvement for hot code paths

[ ] Implement fast VM reset
    - Quick state reset between fuzzing runs
    - Avoid full reboot overhead

LOW PRIORITY
------------
[ ] Port syzkaller to support RV32
    - Syzkaller currently only supports riscv64
    - Would need to add RV32 target, syscalls, executor changes
    - Consider using AFL++ or other fuzzers as alternative

[ ] Add snapshot/restore capability
    - Save/restore full VM state
    - Would speed up fuzzing significantly

[ ] Performance counters
    - Help profile and optimize emulator
    - Useful for debugging performance issues